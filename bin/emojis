#!/bin/bash

# Define an array of emojis
EMOJIS=(⛵️ 🛰 🪐 🚀 🧡 🔭 🌍 💫 ☄️ ✨ 🌠 🌌 ⭐️)

# Generate a random index within the range of the array
EMOJI_INDEX=$(( RANDOM % ${#EMOJIS[@]} ))

# Check the condition
if [ true ]; then
    # Print the random emoji
    echo "${EMOJIS[$EMOJI_INDEX]}"
fi
