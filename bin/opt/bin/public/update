#!/bin/bash

#
#  Update applications
#

echo "🌱 Updating flatpak apps"
flatpak update

echo "🌱 Upgrading system"
rpm-ostree upgrade --check

echo "🌱 Checking node apps"
pushd ~/opt/nodebin >/dev/null || exit
taze major -w && npm i
popd >/dev/null || exit

echo "🌱 Checking rust apps"
rustup check
cargo install-update -a

echo "✨ You have a new shiny machine!"
