#!/bin/bash

#
#  Update applications
#

printf "\n🌱 Updating flatpak apps\n"
flatpak update

printf "\n🌱 Upgrading system\n"
rpm-ostree upgrade --preview

printf "\n🌱 Checking node apps\n"
pushd ~/opt/nodebin >/dev/null || exit
taze major -w && npm i
popd >/dev/null || exit

printf "\n🌱 Checking rust apps\n"
rustup check
cargo install-update -a

printf "✨ You have a new shiny machine!"
