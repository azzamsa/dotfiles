#!/usr/bin/sh

# This file is debian specific.
# Putting the startup app on `.xprofile` does not work

# gnome keyring
eval $(gnome-keyring-daemon --start --daemonize)
export SSH_AUTH_SOCK

# Setup autorand
/home/azzamsa/.config/bspwm/scripts/monitor.sh &

# window compositor
picom &
